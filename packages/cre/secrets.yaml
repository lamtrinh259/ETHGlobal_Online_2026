# Secret IDs the enclave requests with `runtime.getSecret({ id })`, mapped to the
# environment variables that hold their values (simulation) or to Vault DON entries (deployment).
secretsNames:
    REGISTRAR_KEY:
        - SECRET_REGISTRAR_KEY
    VIEWCODE_KEY:
        - SECRET_VIEWCODE_KEY
