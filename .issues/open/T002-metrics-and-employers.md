# Person page: metrics, not judgement — and a page for employers

Asked while reading `/p/test-account-123456`.

## To do

1. [x] **`/p/` stops judging by default.** The checks column repeats the score ring (references,
       name, accounts) and the sections under it (answers, linked accounts). Drop the `incomplete`
       badge, the `Policy: …` line and the ✓/✗ column from the default page: overall metrics only,
       no verdict.
2. [x] **A policy bar.** An autocomplete on the page: type or pick a policy — the presets the
       platform ships, plus the reader's own saved ones — and applying it is what brings the verdict
       and the checks back. A verdict is then something a reader asked for.
3. [ ] **`/employers`.** One page for the side that hires: build a policy, send an invitation, and
       list what came back — who was invited, who answered, and where each stands against the policy.
